name: Erdos-Moser Search Daily

on:
  push:
  schedule:
    - cron: '0 12 * * *'   # Noon UTC (staggered from Beal)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  search:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run Erdos-Moser Search
        run: python erdos_moser.py

      - name: Commit and push progress
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add erdos_moser_state.json erdos_moser_results.jsonl
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto checkpoint: $(date -u +'%Y-%m-%d %H:%M UTC')"
            git pull --rebase origin main
            git push origin main
          fi
